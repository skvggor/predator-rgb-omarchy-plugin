# Agents

## After every commit + push

Run this to update the installed plugin:

```sh
omarchy plugin remove skvggor.predator-rgb --yes && omarchy restart shell && omarchy plugin add git@github.com:skvggor/predator-rgb-omarchy-plugin.git --enable --yes && omarchy restart shell
```
